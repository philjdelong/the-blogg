import '../Title/Title.css'

const Title = (props) => {
    return(
        <div>
            <h1>Welcome to the <code className="title">BLOGG</code></h1>
        </div>
    )
}

export default Title;